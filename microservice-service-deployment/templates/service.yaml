apiVersion: v1
kind: Service
metadata:
  name: {{ template "microservice-service-deployment.fullname" . }}
  labels:
    app: {{ template "microservice-service-deployment.name" . }}
    chart: {{ template "microservice-service-deployment.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "microservice-service-deployment.name" . }}
    release: {{ .Release.Name }}
